pub struct Config {
	pub screen_width: f32,
	pub screen_height: f32,
	pub paddle_width: f32,
	pub paddle_height: f32,
	pub paddle_side_boundary: f32,
	pub top_boundary: f32,
	pub bottom_boundary: f32,
}